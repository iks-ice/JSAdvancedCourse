const insertInMiddle = (array) => {
    console.log(arr.length);
    if (array.length % 2 === 0) {
        // const middlePosition = Math.floor(arr.length / 2);
        // array.splice(middlePosition, 1, element);
        console.log(arr.length);
        // return array;
    }
}
insertInMiddle([1, 2, 3]);