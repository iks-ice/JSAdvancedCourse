const input1 = Number( prompt( 'Input first number', Math.random() * 100 ) );
const input2 = Number( prompt( 'Input second number', Math.random() * 100 ) );
const summmarize = ( a, b ) => console.log( a + b );
