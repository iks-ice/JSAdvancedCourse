const input1 = Number( prompt( 'Input first number', ( Math.random() * 100 ).toFixed( 0 ) ) );
const input2 = Number( prompt( 'Input second number', ( Math.random() * 100 ).toFixed( 0 ) ) );
const summmarize = ( a, b ) => console.log( a + b );
summmarize( input1, input2 );
